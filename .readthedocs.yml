# .readthedocs.yml

version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: miniforge3-latest


sphinx:
  configuration: doc/conf.py

conda:
  environment: doc/doc_env.yaml

python:
  install:
  - path: .
